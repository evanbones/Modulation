### Added

- Added Slot Highlight Behind Item tweak to draw most of the slot hover highlight underneath the item instead of on top
  of it.
    - Backported from 1.21.2, using the same `slot_highlight_back` and `slot_highlight_front`
      sprites under the vanilla paths, so existing resource packs should work as expected.
